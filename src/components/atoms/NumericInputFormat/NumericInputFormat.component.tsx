import React from 'react'
import { Container, NumericInput } from './NumericInputFormat.style'

const NumericInputFormat = (props: any) => (
  <Container>
    <NumericInput {...props} />
  </Container>
)

export default NumericInputFormat
